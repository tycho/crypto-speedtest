# $Id$

TESTS = speedtest

noinst_PROGRAMS = speedtest

speedtest_SOURCES = speedtest.cpp

AM_CFLAGS = -W -Wall $(LIBGCRYPT_CFLAGS)
AM_CXXFLAGS = -W -Wall $(LIBGCRYPT_CFLAGS)
LIBS = $(LIBGCRYPT_LIBS)
