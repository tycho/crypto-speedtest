# $Id$

TESTS = speedtest

noinst_PROGRAMS = speedtest

speedtest_SOURCES = speedtest.cpp

AM_CFLAGS = -W -Wall $(LIBGCRYPT_CFLAGS) $(LIBMCRYPT_CFLAGS) $(BOTAN_CFLAGS)
AM_CXXFLAGS = -W -Wall $(LIBGCRYPT_CFLAGS) $(LIBMCRYPT_CFLAGS) $(BOTAN_CFLAGS)
LIBS = $(LIBGCRYPT_LIBS) $(LIBMCRYPT_LIBS) $(BOTAN_LIBS)
